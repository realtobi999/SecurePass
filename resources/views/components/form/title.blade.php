@props(['text'])
<div>
    <h1 class="font-bold text-center mt-10 mb-6 text-4xl ">SecurePass <span class="text-blue-300 hover:text-blue-600 transition">Vault</span></h1>
    <p class="mt-2 text-m text-gray-500 text-center mb-2">{{ $text }}</p>
</div>
